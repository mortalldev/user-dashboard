import WalletsList from '@/pages/wallets/ui/list';

const Wallets = () => {
    return (
        <div className="w-full h-full">
            <WalletsList />
        </div>
    );
};

export default Wallets;
